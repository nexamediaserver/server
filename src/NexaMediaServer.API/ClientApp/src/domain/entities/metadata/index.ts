/**
 * @module @/domain/entities/metadata
 *
 * Utilities for working with metadata.
 *
 * Includes:
 * - Metadata type classifications
 * - Title formatting
 * - Rating conversions
 *
 * @example
 * ```tsx
 * import { formatTitle, isVideoMetadataType } from '@/domain/entities/metadata'
 * ```
 */

// TODO: Migrate metadata utilities here
// These are currently scattered across features

// Placeholder export to satisfy barrel file
export const METADATA_ENTITY_VERSION = '1.0.0'
