/**
 * @module @/domain/entities/item
 *
 * Utilities for working with media items.
 *
 * Includes:
 * - Image URL generation
 * - Item type helpers
 * - Duration formatting
 *
 * @example
 * ```tsx
 * import { getImageUrl, IMAGE_SIZES } from '@/domain/entities/item'
 *
 * const posterUrl = getImageUrl({
 *   id: item.id,
 *   type: 'poster',
 *   width: IMAGE_SIZES.poster.md,
 * })
 * ```
 */

// TODO: Migrate getImageUrl, image size constants, and item utilities here
// These are currently scattered across features and shared/lib

// Placeholder export to satisfy barrel file
export const ITEM_ENTITY_VERSION = '1.0.0'
