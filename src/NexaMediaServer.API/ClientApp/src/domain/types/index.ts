/**
 * @module @/domain/types
 *
 * Shared TypeScript types for domain concepts.
 *
 * Note: Most types come from GraphQL codegen in `@/shared/api/graphql/graphql`.
 * This module is for additional domain types not covered by the schema.
 *
 * @example
 * ```tsx
 * import type { ItemWithProgress, LibrarySectionFilter } from '@/domain/types'
 * ```
 */

// TODO: Add domain types that extend or supplement GraphQL types
// - ItemWithProgress (item with computed progress percentage)
// - LibrarySectionFilter (filter state for library views)

// Placeholder - nothing to export yet
export type DomainTypesPlaceholder = never
