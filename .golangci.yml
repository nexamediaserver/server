version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - errcheck
  exclusions:
    paths:
      - internal/database/sqlite/sqlcgen/.*

formatters:
  enable:
    - gofmt
  exclusions:
    paths:
      - internal/database/sqlite/sqlcgen/.*
